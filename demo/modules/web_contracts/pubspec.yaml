name: demo_web_contracts
description: Pure Dart route contracts for the shared demo Web container.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.11.5
dependencies:
  ccrouter_contracts: ^0.1.0
dev_dependencies:
  ccrouter_generator: ^0.1.0
