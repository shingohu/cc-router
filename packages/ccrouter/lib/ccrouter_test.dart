/// Test-support bridge consumed by the dedicated `ccrouter_test` package.
///
/// Application and component code must import `ccrouter.dart` instead. This
/// entry point does not grant Runtime ownership to business code.
library;

export 'src/facade.dart' show CCRouterTestBinding;
