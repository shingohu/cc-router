/// Pure-Dart CCRouter registration, lifecycle, and dispatch runtime.
///
/// Framework hosts and low-level Core tests import this library. Flutter
/// business code should import `package:ccrouter/ccrouter.dart` instead.
library;

export 'src/runtime.dart';
