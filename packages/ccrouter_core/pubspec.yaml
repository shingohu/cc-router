name: ccrouter_core
description: Pure Dart runtime for CCRouter.
version: 0.1.0
resolution: workspace
publish_to: none
environment:
  sdk: ^3.9.0
dependencies:
  ccrouter_contracts: ^0.1.0
  meta: ^1.16.0
dev_dependencies:
  test: ^1.26.0
