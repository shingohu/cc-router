name: ccrouter_go_router
description: GoRouter adapter for CCRouter navigation contracts.
version: 0.1.0
resolution: workspace
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  ccrouter: ^0.1.0
  flutter:
    sdk: flutter
  go_router: ^17.5.0

dev_dependencies:
  ccrouter_core: ^0.1.0
  flutter_test:
    sdk: flutter
